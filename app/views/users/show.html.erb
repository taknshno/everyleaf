<h1><%= @user.user_name %>のページ</h1>

<p>メールアドレス: <%= @user.email %></p>

<%= link_to I18n.t('views.messages.delete_user'), user_path(@user.id), method: :delete,
                                                  class: "btn btn-danger btn-sm", data: { confirm: '本当に削除しますか？' } %>